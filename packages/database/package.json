{
  "name": "database",
  "version": "1.0.0",
  "main": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "npx prisma generate",
    "ngrok:start": "yarn db:start && ngrok tcp 5432"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0"
  }
}
