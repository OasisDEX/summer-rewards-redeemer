query DailyRewards($day: ID!, $first: Int, $skip: Int) {
  day(id: $day) {
    id
    borrowDailyRewards(first: $first, skip: $skip) {
      id
      pool {
        id
      }
      user {
        id
      }
      reward
    }
    earnDailyRewards(first: $first, skip: $skip) {
      id
      pool {
        id
      }
      user {
        id
      }
      reward
    }
    week {
      id
    }
  }
}
